//* itérateurs d'array
//* forEach, map, filter, find, reduce
//* itère dans un array - pas besoin de boucle for
//* Accèpte une fonction Callback en argument, appelle le Callback à chaque itération.

// plus vite on maîtrise ces méthodes plus vite on pourra faire des applications JS et React car très souvent utilisées
const numbers = [0, 1, 2, 3, 4];

//* affiche tous les nombres
for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
