//* prepend
//* innerText

const heading = document.createElement('h2');
heading.innerText = 'je suis un titre dynamique';
document.body.prepend(heading);

// on a vu des méthodes similaires juste avant. On verra après une façon plus simple et rapide de créer un élément. On voit plusieurs façons pour le principe
